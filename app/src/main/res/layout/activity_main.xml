<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg002"
    android:padding="20dp"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/iconTv"
        android:src="@drawable/manga_logo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"/>
    
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iconTv"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="Welcome to Manga Book App"
        android:fontFamily="serif"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"/>

    <Button
        android:id="@+id/loginadminbtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/loginusrbtn"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#FE2F52"
        android:text="Login as Admin"
        android:textColor="@color/white"
        app:cornerRadius="50dp"
        tools:ignore="ExtraText" />

    <Button
        android:id="@+id/loginusrbtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/skipbtn"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#FE2F52"
        android:text="Login as User"
        android:textColor="@color/white"
        app:cornerRadius="50dp"
        tools:ignore="ExtraText" />

    <Button
        android:id="@+id/skipbtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Continue Without Login"
        android:backgroundTint="#FE2F52"
        android:textColor="@color/white"
        app:cornerRadius="50dp"
        tools:ignore="ExtraText"/>

</RelativeLayout>